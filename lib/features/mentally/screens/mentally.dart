import 'package:flutter/material.dart';

class MentallyScreen extends StatelessWidget {
  const MentallyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Mentally Screen"),
    );
  }
}
